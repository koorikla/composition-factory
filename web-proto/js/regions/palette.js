/**
 * Region: PALETTE (left rail). Root element: #region-palette (see index.html).
 * STUB — owned by the palette region agent; replace this file's body.
 * Contract: import { store } from "../store.js" and subscribe to topics
 * ("doc", "selection", "generate", "error"). Kind listings/fields come from
 * ../api.js (getKinds, getKindFields). Do not edit store.js/api.js/wires.js.
 */
export {};
