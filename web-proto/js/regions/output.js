/**
 * Region: OUTPUT (bottom drawer). Root element: #region-output
 * (tabs: #tabs, meta: #meta, warnbar: #warn, code: #code).
 * STUB — owned by the output region agent; replace this file's body.
 * Contract: import { store } from "../store.js" and subscribe to topics
 * ("doc", "selection", "generate", "error"). Generated output arrives on the
 * "generate" topic ({outputs:[{path,bytes,body}], written}) after
 * store.generate(). Do not edit store.js/api.js/wires.js.
 */
export {};
