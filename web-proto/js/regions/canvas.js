/**
 * Region: CANVAS. Root element: #cw (contains #wires svg and #canvas layer).
 * STUB — owned by the canvas region agent; replace this file's body.
 * Contract: import { store } from "../store.js" and subscribe to topics
 * ("doc", "selection", "generate", "error"). Wires are derived from the doc
 * via ../wires.js (listWires, fanOut). Node positions live in
 * store.state.positions via store.setPosition/getPosition (client-only).
 * Do not edit store.js/api.js/wires.js.
 */
export {};
