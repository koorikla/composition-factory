apiVersion: v1
kind: Service
metadata:
  name: compositionfactory
  labels:
    app.kubernetes.io/name: compositionfactory
    app.kubernetes.io/component: canvas
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: compositionfactory
