# Generated by compositionfactory. Do not edit.
# Source: ghcr.io/crossplane-contrib/provider-aws-elasticache:v2.7.0
# Regenerate with: cf gen
# ASSUMPTION: no ClusterProviderConfig CRD for provider family "aws" was found among the loaded schemas -- only its service package(s) are cached, not provider-family-aws itself. What follows is the well-known Upbound v2 family shape (<family>.m.upbound.io/v1beta1, kind ClusterProviderConfig), not a value read from a real CRD. Run `cf provider add xpkg.upbound.io/upbound/provider-family-aws` and regenerate to replace this guess with the real apiVersion/kind.
apiVersion: aws.m.upbound.io/v1beta1
kind: ClusterProviderConfig
metadata:
  name: example  # must match this family's XR(s): the value passed for the providerName parameter
spec:
  credentials:
    source: Secret  # your provider may also support IRSA/WebIdentity/InjectedIdentity/etc; see its docs
    secretRef:
      namespace: crossplane-system  # TODO: namespace holding the credentials Secret
      name: aws-creds  # TODO: name of the credentials Secret
      key: creds  # TODO: key inside that Secret holding the provider's credentials
