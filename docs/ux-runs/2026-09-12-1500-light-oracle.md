# Light oracle run — 2026-09-12 15:00 UTC

Slot `(12*4 + 15/7) % 3 = 2`. Mode: **light**, 4-hour window (11:00–15:00 UTC).
Base: `origin/main` @ `d947002`. Engine tested on port **8090**, seeded from
`tests/fixtures/cache` (`provider-aws-s3` and `provider-aws-sqs`).

Driver path: scripted Playwright against loopback `cf serve`.

---

## 1. Quality Gates

| Gate | Command | Result |
|---|---|---|
| `make lint` | `go vet ./...`, `eslint web-proto/js/`, `tsc -p jsconfig.json` | **PASS** (clean) |
| `make lint-strict` | `staticcheck@v0.8.1` | **PASS** (clean) |
| `go test -short` | `go test ./... -short -count=1` | **PASS** (~4.5s) |
| `govulncheck` | `go run golang.org/x/vuln/cmd/govulncheck@latest ./...` | **PASS** (0 vulnerabilities) |
| CI on `main` | Run 34700564503 @ `d947002` | **PASS** (`success`) |

---

## 2. Verification — Closures of the Last 4 Hours

Window 11:00–15:00 UTC. **21 issues / 23 landed commits** on `main`:

| id | commit | description | guarding test |
|---|---|---|---|
| CF-455 (#348) | `d947002` | reject nested environment paths in patches and templates | `internal/adopt/adopt_test.go` (+106) |
| CF-449 (#341) | `d728d8f` | unpack mapStructured object parameters in KCL & Python | `internal/emit/cf449_map_structured_test.go` (+332) |
| CF-456 (#349) | `c8e8483` | reject nested parameter references in when/forEach guards | `internal/adopt/adopt_test.go` (+100) |
| CF-453 (#346) | `b3e0094` | wrap native resources and metadata in `_present` for Python | `internal/emit/python_test.go` (+179) |
| CF-458 (#352) | `70f5fb4` | validate root fields, metadata, required spec on MRs | `internal/emit/cf458_managed_validate_test.go` (+160), `render_validate_test.go` (+89) |
| CF-454 (#347) | `de18a5b` | recognize nested hasKey guards in XRD-less adopt | `internal/adopt/xrdless_test.go` (+137) |
| CF-445 (#337) | `ec03f2d` | resolve metadata.name wires to byte targets across engines | `internal/emit/structured_test.go` (+173), `python_test.go` (+104), `kcl_test.go` (+68) |
| CF-442 (#334) | `8059746` | populate optional object properties for expression preview | `internal/emit/preview_test.go` (+84), `sample_xr_test.go` (+42) |
| CF-438 (#330) | `f98282f` | decouple server startup from blocking OCI source fetches | `internal/api/server_test.go` (+191), `cf088_on_demand_source_test.go` (+15) |
| CF-429 (#321) | `dccbf32` | rewrite raw field expressions on resource name normalization | `internal/adopt/adopt_test.go` (+381) |
| CF-395 (#286) | `ba00827` | emit and render wires for forEach loop bounds | `tests/canvas.spec.js` (+173) |
| CF-427 (#319) | `41071dc` | fallback for optional parameter/status metadata.name wires | `internal/emit/python_test.go` (+198), `kcl_test.go` (+131), `composition_test.go` (+81) |
| CF-452 (#345) | `2595ce7` | merge native metadata.annotations into AnnPlan | `internal/emit/native_test.go` (+207) |
| CF-417 (#307) | `7c6cb83` | suppress status output ports on kinds lacking status schema | `tests/canvas.spec.js` (+46) |
| CF-339 (#228) | `95c147f` | support getComposedResource in status-driven forEach loops | `internal/adopt/adopt_test.go` (+271) |
| CF-358 (#250) | `a3ed000` | include raw parameter references in fanOut count | `tests/wires.spec.js` (+145) |
| CF-451 (#343) | `2b234d1` | accept object parameters wired to native resource map leaves | `internal/blueprint/validate_resources_test.go` (+137) |
| CF-335 (#224) | `7e7c87b` | support default wrappers in status forEach (re-landed) | `internal/adopt/adopt_test.go` (+172) |
| CF-423 (#316) | `ef5881c` | clear templateSource on non-Go engines | `tests/output-drawer.spec.js` (+97) |
| CF-337 (#226) | `610cc41` | recognize getComposedResource status references as wires | `internal/adopt/adopt_test.go` (+334) |
| CF-408 (#299) | `6f6a1f0` | confirm/unwire fields on parameter type changes | `tests/inspector.spec.js` (+332) |

### Re-verification Execution
- Targeted Go tests across `internal/adopt`, `internal/emit`, `internal/api`, and `internal/blueprint`: **all passed**.
- Playwright specs covering the UX issues in the window (`tests/canvas.spec.js`, `tests/inspector.spec.js`, `tests/output-drawer.spec.js`, `tests/wires.spec.js`): **15 passed (5.8s)** under gate lock.

### Residue & Anomaly Check
- **Issue #330 (CF-438)**: Commit `f98282f` landed cleanly on `main` ("fix(api): decouple server startup and request serving from blocking OCI source fetches (CF-438, #330)"), but the issue remains **OPEN** on GitHub. Per oracle guidelines, oracle does not close issues; left open for the driver to close.
- **Reverted and re-landed fixes**: CF-442 (`877bc7c` → revert `570418e` → re-land `8059746`) and CF-335 (`3052d26` → revert `caf3e8e` → re-land `7e7c87b`) were both cleanly settled on `main`, guarded by comprehensive tests, and pass.

---

## 3. Mission M1 — First Contact: S3 Bucket to Generate

**Outcome: completed.** Blank start on port 8090 with scratch dir `.testrun-ux` and cache seeded from `tests/fixtures/cache`.

### Narrative & Execution
1. **Arrival**: On blank start, canvas displays `#examplesOverlay` starter modal. Dismissed cleanly via Escape.
2. **Palette Search**: Filtered kinds via `#psearch` typing `s3`. Palette instantly filtered to S3 kinds from the cached provider.
3. **Canvas Drop**: Dragged `Bucket` from `#lrail .kind[data-kind="Bucket"]` onto canvas. Bucket node created on canvas and `provider-aws-s3` declared as source.
4. **Parameter Definition**: Under SHARED rail, opened `#param-add-btn`, added parameter `region` (type `string`, optional).
5. **Wiring**: Wired parameter `region` to `bucket.spec.forProvider.region`.
6. **Generate**: Clicked `Generate`, accepted native confirm dialog (`Generate will write manifests to disk in '.testrun-ux/out'. Proceed?`). Manifests emitted:
   - `compositions/xapps.platform.example.org.yaml`
   - `xrds/xapps.platform.example.org.yaml`
   - `providerconfigs/aws.yaml`
   - `functions.yaml`
7. **Oracle & Round-Trip**:
   - Emitted Composition has proper `hasKey $spec "region"` guard and quote formatting.
   - Run `./bin/cf adopt` on emitted composition: reproduced the original blueprint resources, sources, and wire `from: params.region` with 0 dropped fields.
   - Port 8090 stopped and freed cleanly.

---

## 4. Dependabot PR Audit

- `gh pr list --search "author:app/dependabot"`: **0 open PRs**.
- Only prior oracle run PRs (#354, #351, #344, #308, #277, #233, #194) are open awaiting maintainer review.

---

## 5. Findings

**0 new defect findings.**
- CF-459 (favicon 404) and CF-460 (e2e spec cf086 pulling unseeded RDS provider) are already filed and tracked.
- Validate remains unavailable due to absence of Docker daemon in environment (CF-093).
- CF-438 / issue #330 residue noted above.

---

## 6. Port Hygiene & Teardown

- Port 8090 was used exclusively for the isolated test run and stopped immediately after mission completion.
- Scratch files under `.testrun-ux` removed.
- Worktree clean.
