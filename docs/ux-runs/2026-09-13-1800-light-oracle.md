# Light oracle run — 2026-09-13 18:00 UTC

Slot: `(UTC_day*4 + UTC_hour/7) % 3` = `(256*4 + 18/7) % 3` = `(1024 + 2) % 3` = 0 → **M1** (first contact, S3 bucket, parameter wiring). Started 18:00:00 UTC, cap 75 min.
Checkout: `2c2fa01` (origin/main). Engine port **8090**. Mission carried to Generate.

Oracle only: no code fixed, no issue closed, no `verified`/`in-progress` label added, nothing pushed to `main`.

---

## 1. Gates

| gate | here | main CI (run 34766744053, `2c2fa01`) | verdict |
|---|---|---|---|
| `make lint` | **pass** (`gofmt` & `go vet` clean) | success | green |
| `make lint-strict` | **pass** (`staticcheck@v0.8.1` clean) | success | green |
| `go test -short -count=1 ./...` | **pass** (all packages passed) | success | green |

Main CI run 34766744053 concluded `success` across all jobs (`test`, `acceptance`, `cluster`, `e2e`, `docker-build`). Local gates all pass without warnings or errors.

---

## 2. Verification of recent closes

Window: issues closed in the past 4 hours (`14:00:20 UTC` to `18:05:00 UTC`) — **5 issues**.
All 5 were verified against their guarding tests and live behavior. No regressions, half-fixes, or residues found.

| issue | id | title | how verified |
|---|---|---|---|
| #392 | CF-481 | When a status-wired resource is conditionally omitted, downstream resources evaluate unobserved status as empty string | Guard test `internal/emit/TestStatusWireGatesDependentResourceWhenUnobserved` in `status_wire_test.go` passes cleanly. |
| #391 | CF-480 | RDS starter example fails validation on writeConnectionSecretToRef username/password keys | Guard tests `TestRDSStarterConfiguresUsernameAndPasswordSecretRef` and `TestConfigMapRDSBlueprintHasUsernameAndPasswordSecretRef` in `internal/examples/examples_test.go` pass cleanly. |
| #388 | CF-479 | Canvas bottom drawer artifacts tree labels the served blueprint after its metadata.name instead of its file name | Guard test `tests/cf479-artifacts-tree-blueprint-label.spec.js` passes cleanly; independently verified in the live canvas during Mission M1 (tree item displays `doc.cf.yaml`). |
| #387 | CF-478 | Annotation wires targeting a field that already has a template-generated wire produce duplicate Go template keys | Guard test `tests/slice37-annotation-wires.spec.js` passes cleanly. |
| #386 | CF-477 | Status wires referencing non-existent status properties on the source resource are silently accepted | Guard tests `TestXRDStatusProperties`, `TestStatusWireXRStatusRendersObservedValue`, `TestValidateXRDStatus`, and `TestValidateRejectsInvalidStatusWire` in `internal/emit/` pass cleanly. |

Also verified live during Mission M1:
- **CF-475 (#382)**: Inline new XRD parameter form (`+ new XRD parameter…`) dismissal after clicking `Add`. In previous versions, the input form lingered. In this run, the form was completely dismissed upon adding `region` and replaced by the active bound chip (`params.region`).

---

## 3. Mission — M1, first contact (S3 Bucket & Parameter Wiring)

**Goal.** Blank document, discovery of S3 provider via catalogue search, installing provider from cache, placing a `Bucket` resource on the canvas, wiring its `region` field to a new XRD parameter via the inspector, verifying UI feedback and dismissal, and carrying the document through `Generate` to manifests on disk.

**Environment Notes.**
Offline/cloud constraints: Egress denies `ghcr.io` blob CDN downloads. Schema cache seeded from `tests/fixtures/cache` into `.testrun-ux/cache` (`provider-aws-s3` and `provider-aws-sqs`). Port 8090 used for the engine (`./bin/cf serve --addr 127.0.0.1:8090 --blueprint .testrun-ux/doc.cf.yaml --out .testrun-ux/out --lock .testrun-ux/.cf.lock --cache-dir .testrun-ux/cache`). Mission carried to Generate; Validate chip not exercised as Docker is absent in this cloud environment (CF-093).

**Visual Journey (19 Screenshots).**
All screenshots captured in `docs/ux-runs/2026-09-13-1800-light-oracle/`:

1. `01-arrival.png`: Canvas arrival on blank document with starter chooser modal (`#examplesOverlay`) open.
2. `02-blank-canvas.png`: Starter chooser dismissed, displaying clean canvas with `#canvas-empty-state` onboarding guidance.
3. `03-search-kinds-s3-empty.png`: Kinds search for `s3` before provider installation showing helpful empty state prompting user to check SOURCES.
4. `04-search-kinds-bucket-empty.png`: Kinds search for `bucket` showing empty state with link to SOURCES catalogue.
5. `05-sources-tab.png`: Switched to SOURCES tab showing catalogue search and installed packages list.
6. `06-search-cat-s3.png`: Catalogue search for `s3` filtering to `provider-aws-s3`.
7. `07-provider-installing.png`: Clicking `Add` on `provider-aws-s3`, showing installation spinner/progress.
8. `08-provider-installed.png`: `provider-aws-s3` successfully installed in left rail (`#lrail .src-row`).
9. `09-kinds-tab.png`: Switched back to KINDS tab, populated with providers' resources.
10. `10-kinds-search-bucket.png`: Filtered kinds for `bucket`, showing `Bucket` from `s3.aws.m.upbound.io/v1beta1`.
11. `11-bucket-placed.png`: Synthetic HTML5 drag-and-drop placing `Bucket` onto canvas `#cw`, generating resource node `bucket`.
12. `12-inspector-initial.png`: Clicked `bucket` node header to open right inspector panel.
13. `13-fields-view.png`: Inspector essentials showing `region` (string) with value `us-east-1` and `Val`, `Wire`, `Raw` mode buttons.
14. `14-wire-mode-active.png`: Toggled `Wire` mode on `region`, rendering wire target dropdown `select[data-wire="region"]`.
15. `15-new-param-form-expanded.png`: Selected `+ new XRD parameter…` (`__new__`), expanding inline form with name input, type selector, and Add button.
16. `16-region-param-wired.png`: Entered parameter name `region` and clicked `Add`. Verified CF-475 fix (form dismissed immediately) and verified bound chip `$region → bucket.region` pointing to `params.region`.
17. `17-canvas-settled.png`: Background click settling canvas selection and updating resource wire visuals.
18. `18-artifacts-tree.png`: Bottom drawer artifacts tree open, verifying CF-479 fix (blueprint node labelled `doc.cf.yaml` matching served filename instead of `untitled.cf.yaml`).
19. `19-after-generate.png`: Clicked `Generate`, handled confirmation dialog, output banner updated to `✓ Output written to .testrun-ux/out · Apply: kubectl apply -R -f .testrun-ux/out`.

**Manifests Verified on Disk.**
All manifests cleanly emitted in `.testrun-ux/out`:
- `xrds/xapps.platform.example.org.yaml`: XRD with `providerName` and newly wired parameter `region` of type `string`.
- `compositions/xapps.platform.example.org.yaml`: Composition with go-templating pipeline mapping `{{ $spec.region | quote }}` to `Bucket.spec.forProvider.region` guarded by `hasKey`.
- `functions.yaml`: Pinned function dependencies (`function-go-templating:v0.12.0`, `function-auto-ready:v0.5.0`).
- `providerconfigs/aws.yaml`: `ClusterProviderConfig` scaffold for AWS family.

**Console & Runtime Errors.**
- Console errors: 0
- Page errors: 0
- Unhandled rejections: 0

---

## 4. Issues

0 issues filed.

All tested workflows completed smoothly without defect:
- Search empty states behave predictably.
- Provider catalogue installation works reliably from cache.
- Drag-and-drop resource creation on canvas functions without DOM anomalies.
- Inspector essentials and wire binding mechanisms operate cleanly.
- Recent bugfixes (CF-475, CF-477, CF-478, CF-479, CF-480, CF-481) hold with zero regressions.

---

## 5. Dependabot

Audited open Dependabot pull requests via `gh pr list --search "author:app/dependabot"`:
- **0 open PRs**.
No action required.

---

## 6. Housekeeping Note

No ID collisions or stale links detected. Next free ID remains open for the next brief.
All quality gates and runtime behaviors are clean.
