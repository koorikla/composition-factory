# Light oracle run — 2026-09-13 00:07 UTC

Slot: `(day*4 + hour/7) % 3` = `(13*4 + 0/7) % 3` = 52 % 3 = 1 → **M2 (The status wire: IRSA)**. Started 00:05 UTC, cap 75 min.
Checkout: `3433359` (`origin/main`). Engine port **22676**. Mission carried to Generate.

Oracle only: no code fixed, no issue closed, no `verified`/`in-progress` label added, nothing pushed to `main`.

---

## 1. Gates

| Gate | Local Run | Main CI (run 34722125444, `3433359`) | Verdict |
|---|---|---|---|
| `make lint` | **pass** (`gofmt`, `go vet`, `lint:js`, `typecheck`) | success | green |
| `make lint-strict` | **pass** (`staticcheck@v0.8.1`) | success | green |
| `go test -short -count=1` | **pass** (all packages in ~8s) | success | green |

Baseline validation suite passed with zero errors or warnings. Main CI is fully green across all 5 jobs (`acceptance`, `cluster`, `docker-build`, `test`, `e2e`).

---

## 2. Verification of Recent Closes

Window: issues closed in the previous 4-5 hours (7 issues total). Every closed item was re-verified against its guarding test suite and live behavior on `main`.

| Issue | PR / Commit | Title / Component | How Verified | Verdict |
|---|---|---|---|---|
| **#370** | CF-472 (`3433359`) | fix(cli): defer undeclared cached providers when blueprint sources are declared | `go test ./internal/emit -run TestAssembleProviders` & Playwright `tests/cf465-palette-background-providers.spec.js` pass. Tested live in M2 run: adding provider-aws-sqs to sources only activates SQS and does not leak unselected cached providers into sources. | Verified |
| **#364** | CF-467 (`e3e7845`) | fix(web-proto): installed providers list in sources tab shows cached providers not declared in sources | Playwright `tests/cf467-sources-installed-providers.spec.js` passes. Verified live in M2 run: Sources tab correctly differentiates between Catalogue ("Add" button for uninstalled cached providers) and Installed list. | Verified |
| **#365** | CF-468 (`7300f89`) | fix(web-proto): render field value properly in inspector | Playwright `tests/canvas.spec.js` passes. Verified live: Inspector field rows render current values and handle empty/placeholder states cleanly. | Verified |
| **#363** | CF-466 (`dbead31`) | fix(adopt): handle contradicting values in adopted manifests | `go test ./internal/adopt -run CF466` passes. Verified conflicting resource configurations correctly generate a loss report without crashing. | Verified |
| **#361** | CF-465 (`6b51c14`) | fix(web-proto): palette background provider fetching | Playwright `tests/cf465-palette-background-providers.spec.js` passes. Verified live: palette populates and schemas load asynchronously without blocking UI interactions. | Verified |
| **#360** | CF-464 (`4a6d450`) | fix(api): background provider caching | `go test ./internal/api` background fetch tests pass. Cache directory updates do not collide with active server reads. | Verified |
| **#19** | CF-133 (`a8894df`) | fix(cluster): improve kind cluster lifecycle handling | `go test ./internal/cluster` passes. Kind cluster startup, teardown, and label validation hold. | Verified |

No regressions, broken assertions, or residual defects found across any recently closed issues.

---

## 3. Canvas UX Mission M2: The Status Wire (IRSA)

Mission **M2** exercises status-to-field wiring: establishing an unprompted `hasKey` guard in Go-templating when wiring a runtime status output (AWS SQS Queue ARN) to an annotation on a Kubernetes ServiceAccount (`eks.amazonaws.com/role-arn`), then validating end-to-end emission to disk and round-trip adoption.

- **Workspace Port**: `22676` (isolated testrun directory `.testrun-48c14124`)
- **Fixtures**: Seeded provider cache from `tests/fixtures/cache` (includes `provider-aws-sqs:v2.7.0` and `k8s`)
- **Runner**: Playwright automated script under the naive-eye rule, recording browser console logs, HTTP traffic, and screenshots.

### Step 1: Blank Arrival & Starter Chooser
- Launched `cf serve` on blank blueprint. The starter blueprint overlay (`#examplesOverlay`) rendered automatically.
- Verified 11 starter blueprint cards rendered with titles, tags, and ready status badges:
  - *Cloud-Agnostic Web Workload*, *Cloud-Agnostic Scheduled CronJob*, *AWS S3 Secure Storage Bucket*, *AWS SQS Queue with Dead Letter Queue*, *Cloud-Agnostic Portable Database*, *External Secrets Operator Integration*, *AWS IRSA (IAM Role + EKS ServiceAccount)*, *AWS RDS PostgreSQL Database*, *Full-Stack Microservice*, *Google Cloud Storage Bucket*, *Azure Flexible Server PostgreSQL*.
- Closed modal via `#examplesCloseBtn`.
- Screenshot: `01-blank-arrival.png`, `02-modal-closed.png`.

### Step 2: Empty Canvas State
- Canvas displayed empty state guidance:
  `"1. Drag kinds from KINDS 2. Add cloud providers in SOURCES1 Drag kinds onto canvas from KINDS (16 native Kubernetes kinds ready without providers)2 Add cloud providers in SOURCES for AWS, Azure, GCP"`
- 16 native Kubernetes kinds immediately available in KINDS without requiring external providers.

### Step 3: Sources Tab & Catalogue Search Ranking (CF-140 Verification)
- Switched to SOURCES tab (`#rtabs button[data-r="src"]`).
- Searched `iam` in `#cat-search`:
  - Exactly as specified by CF-140, exact name matches ranked first (`provider-aws-iam`, `provider-gcp-iam`), unannotated.
  - Description matches (`provider-gcp-artifact`, `provider-gcp-cloudfunctions`, etc.) ranked below with explicit `Matches description: GCP Provider for Crossplane. — ...` labels.
- Searched `sqs` in `#cat-search`:
  - `provider-aws-sqs:v2.7.0` rendered with `Add` button.
  - Clicked `Add`: status updated to `Installed · 8 kinds`, and schema was loaded into palette.
- Screenshot: `03-sources-tab.png`, `04-sources-iam-search.png`, `05-sources-sqs-search.png`, `06-sqs-added.png`.

### Step 4: KINDS Tab & Resource Placement
- Switched to KINDS tab.
- Dragged `Queue` (`sqs.aws.m.upbound.io/v1beta1`) onto canvas at `(250, 200)`.
  - Queue node rendered with name `queue`, required parameter `region* string`, and 6 status output ports with green dots:
    - `name / id` (`status.atProvider.id`)
    - `arn` (`status.atProvider.arn`)
    - `contentBasedDeduplication` (`status.atProvider.contentBasedDeduplication`)
    - `deduplicationScope` (`status.atProvider.deduplicationScope`)
    - `delaySeconds` (`status.atProvider.delaySeconds`)
    - `fifoQueue` (`status.atProvider.fifoQueue`)
- Dragged `ServiceAccount` (`v1`) onto canvas at `(650, 200)`.
- Screenshot: `07-kinds-tab.png`, `08-queue-dropped.png`, `09-serviceaccount-dropped.png`.

### Step 5: ServiceAccount Inspector & Empty Annotation Validation (CF-136 Verification)
- Selected `ServiceAccount` node: Inspector opened on right rail showing `ServiceAccount service-account`, `15 leaf fields · 0 required`, and `ANNOTATIONS` section.
- Tested CF-136 empty value validation:
  - Entered key `eks.amazonaws.com/role-arn` and left value empty.
  - Clicked `Add`: rejected as expected.
  - Toast banner displayed: `Annotation "eks.amazonaws.com/role-arn" requires a value (or a temporary placeholder to wire later)`.
  - Inspector warning bar displayed identical guidance.
  - The key input field preserved `eks.amazonaws.com/role-arn` without erasing user input.
- Entered temporary placeholder `placeholder` and clicked `Add`.
  - Annotation port row `annotations.eks.amazonaws.com/role-arn` appeared on the ServiceAccount canvas card.
- Screenshot: `10-sa-selected.png`, `11-annotation-empty-attempt.png`, `12-annotation-added.png`.

### Step 6: Drag-to-Wire Status ARN to Annotation (CF-139 Verification)
- Initiated mouse drag from Queue `arn` output port dot `(x: 722, y: 408.75)` to ServiceAccount `eks.amazonaws.com/role-arn` input port dot `(x: 773, y: 369.75)`.
- SVG wire connected immediately on mouse release (`svg.wires path.wire-path` count: 1).
- Wire title tooltip: `queue.status.atProvider.arn → service-account.annotations.eks.amazonaws.com/role-arn`.
- ServiceAccount Inspector displayed full untruncated binding detail (CF-139):
  `Annotationseks.amazonaws.com/role-arn← resources.queue.status.atProvider.arn×queue.status.atProvider.arn → service-account.annotations.eks.amazonaws.com/role-arn`
- Screenshot: `13-wire-connected.png`.

### Step 7: Template Preview Drawer & Guard Verification
- Inspected the bottom drawer composition template preview.
- The engine automatically synthesized the unprompted `hasKey` guard to prevent render crashes during initial provisioning before the queue ARN exists in observed status:
  ```yaml
  annotations:
    {{ setResourceNameAnnotation "service-account" }}
    {{- if hasKey (dig "resources" "queue" "resource" "status" "atProvider" dict $.observed) "arn" }}
    'eks.amazonaws.com/role-arn': {{ (index $.observed.resources "queue").resource.status.atProvider.arn | quote }}
    {{- end }}
  ```
- Screenshot: `14-template-preview.png`.

### Step 8 & 9: Generate & Disk Emission
- Clicked `#generateBtn`. Native browser confirm dialog prompted:
  `Generate will write manifests to disk in '/Users/kaurkallas/compositionfactory/.testrun-48c14124/out'. Proceed?`
- Dialog accepted. Top bar updated to `written · 4 files · 3 previews`.
- Verified manifests written to disk:
  - `out/compositions/xapps.platform.example.org.yaml` (1,706 bytes)
  - `out/xrds/xapps.platform.example.org.yaml` (916 bytes)
  - `out/providerconfigs/aws.yaml` (1,144 bytes)
  - `out/functions.yaml` (579 bytes)
- Output banner presented copyable commands:
  - `Apply: kubectl apply -R -f .../out`
  - `Package: cf package`
- Screenshot: `15-generated.png`.

### Step 10: Round-Trip Check via `cf adopt`
- Ran adoption on the generated output directory:
  ```bash
  ./bin/cf adopt .testrun-48c14124/out --out .testrun-48c14124/adopted.cf.yaml
  ```
  - Exit code: `0` (Zero loss, no dropped fields).
- Diff between the canvas blueprint (`doc.cf.yaml`) and adopted blueprint (`adopted.cf.yaml`):
  ```diff
  --- .testrun-48c14124/doc.cf.yaml
  +++ .testrun-48c14124/adopted.cf.yaml
  @@ -1,7 +1,7 @@
   apiVersion: factory.crossplane.io/v1alpha1
   kind: Blueprint
   metadata:
  -  name: untitled
  +  name: xapps.platform.example.org
  ```
  All resources, kinds, fields, wire bindings (`from: resources.queue.status.atProvider.arn`), parameters, and sources were 100% recovered.
- Re-generated manifests from `adopted.cf.yaml` via `./bin/cf gen`:
  - Diff against original `out/` manifests showed only `# Source: xapps.platform.example.org` comment updates.
  - All Kubernetes and Crossplane YAML specifications are byte-identical.
  - The Round-Trip Rule is completely satisfied.

---

## 4. Dependabot PR Audit

Queried open Dependabot pull requests:
```bash
gh pr list --search "author:app/dependabot" --json number,title,headRefName,statusCheckRollup,mergeable
```
Result: **0 open Dependabot PRs**. No pending dependency updates or security advisory alerts.

---

## 5. Summary & Filed Issues

- **Gates**: All green (`make lint`, `make lint-strict`, `go test -short`).
- **Closures Verified**: 7 issues (#370, #364, #365, #363, #361, #360, #19) verified clean on `main`.
- **Mission M2**: Carried fully through Generate, Disk Inspection, and Round-Trip Adoption. All contracts held.
- **Issues Filed**: **0** (no defects, regressions, or unexpected behaviors discovered).
