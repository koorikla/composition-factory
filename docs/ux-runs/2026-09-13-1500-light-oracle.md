# Light oracle run — 2026-09-13 15:00 UTC

Slot: light run. Started 15:00 UTC, cap 75 min.
Checkout: `4805568` (`origin/main`). Engine port **8090** (scratch dir `.testrun-ux`). Mission **M1** carried to Generate.

Oracle only: no code fixed, no issue closed, no `verified`/`in-progress` label added by this oracle, nothing pushed to `main`.

---

## 1. Gates

| gate | here | main CI (run 34762780840, `4805568`) | verdict |
|---|---|---|---|
| `make lint` | **pass** (`LINT_EXIT=0`) | success | green |
| `make lint-strict` | **pass** (`STRICT_EXIT=0`) | success | green |
| `go test -short ./...` | **pass** (`TEST_EXIT=0`, 0 failures) | success | green |

All preflight gates clean. GitHub Actions run 34762780840 on `origin/main` is 100% green across all 5 jobs (`lint`, `unit-race`, `acceptance`, `cluster`, `e2e`). Dependabot: see §5 — 0 open PRs.

---

## 2. Verification of recent closes

Window: issues closed in the 4-hour window (11:00 UTC to 15:00 UTC). **2 issues** closed in this window. Both verified against reproduction tests.

### Verified fixed — engine & examples

| issue | id | how verified | verdict |
|---|---|---|---|
| #386 | CF-477 | Unit tests in `internal/emit` and `internal/blueprint` | **VERIFIED FIXED** |
| #383 | CF-476 | Unit tests in `internal/examples` and docker acceptance test `TestAcceptanceAllStarterExamplesRender/cloud-database` | **VERIFIED FIXED** |

#### CF-477 (#386) — Blueprint IR and emit support for composite resource definition status schemas and status mappings
Closed 14:35:15Z in commit `7e8b628`.
- Added `status` and `statusWire` support to blueprint IR and emission engine (`internal/emit/xrd.go`, `internal/emit/render.go`).
- Guarding tests executed:
  ```console
  $ go test -v ./internal/emit -run 'TestXRDStatusProperties|TestStatusWireXRStatusRendersObservedValue'
  === RUN   TestXRDStatusProperties
  --- PASS: TestXRDStatusProperties (0.00s)
  === RUN   TestStatusWireXRStatusRendersObservedValue
  --- PASS: TestStatusWireXRStatusRendersObservedValue (0.00s)
  PASS
  ok  	github.com/koorikla/compositionfactory/internal/emit	0.012s

  $ go test -v ./internal/blueprint -run 'TestValidateXRDStatus|TestValidateRejectsInvalidStatusWire'
  === RUN   TestValidateXRDStatus
  --- PASS: TestValidateXRDStatus (0.00s)
  === RUN   TestValidateRejectsInvalidStatusWire
  --- PASS: TestValidateRejectsInvalidStatusWire (0.00s)
  PASS
  ok  	github.com/koorikla/compositionfactory/internal/blueprint	0.009s
  ```
- Result: **VERIFIED FIXED**. The status fields and status wires are validated and emit into Crossplane XRD status OpenAPI schema and Composition pipeline step patches.

#### CF-476 (#383) — Portable Database starter still carries its master password in the XR's own spec, now as a required plaintext field
Closed 12:41:04Z in commit `4805568`.
- Replaced the required XR spec `password` parameter on `cloud-database` with an environment configuration variable `POSTGRES_PASSWORD`, preventing plaintext credential exposure on the composite resource.
- Guarding tests executed:
  ```console
  $ go test -v ./internal/examples -run 'TestStarterExamplesHaveNoPlaintextCredentialsInXRD|TestPortableDatabaseStarterObtainsPasswordFromEnvironmentNotXRSpec'
  === RUN   TestStarterExamplesHaveNoPlaintextCredentialsInXRD
  --- PASS: TestStarterExamplesHaveNoPlaintextCredentialsInXRD (0.00s)
  === RUN   TestPortableDatabaseStarterObtainsPasswordFromEnvironmentNotXRSpec
  --- PASS: TestPortableDatabaseStarterObtainsPasswordFromEnvironmentNotXRSpec (0.00s)
  PASS
  ok  	github.com/koorikla/compositionfactory/internal/examples	0.015s

  $ go test -v -run 'TestAcceptanceAllStarterExamplesRender/cloud-database' .
  === RUN   TestAcceptanceAllStarterExamplesRender
  === RUN   TestAcceptanceAllStarterExamplesRender/cloud-database
  --- PASS: TestAcceptanceAllStarterExamplesRender (2.95s)
      --- PASS: TestAcceptanceAllStarterExamplesRender/cloud-database (0.28s)
  PASS
  ok  	github.com/koorikla/compositionfactory	3.142s
  ```
- Result: **VERIFIED FIXED**. The starter emits without any plaintext password field in the XRD, and Crossplane composition render validates cleanly.

*(Note on #380 / CF-474: closed at 11:17:57Z as a half-fix noted by the 11:00 oracle run, which was then superseded and fully fixed by CF-476 above).*

---

## 3. Mission — M1, first contact: an S3 bucket, from nothing

**Goal.** Arrive at a blank canvas, locate and install `provider-aws-s3`, place a `Bucket` resource, declare and wire a `region` parameter, and carry through to Generate.

**Execution Environment.**
- Server: `./bin/cf serve --addr 127.0.0.1:8090 --blueprint .testrun-ux/doc.cf.yaml --out .testrun-ux/out --lock .testrun-ux/.cf.lock --cache-dir .testrun-ux/cache`
- Automation: Playwright headless browser script (`.testrun-ux/driver.js`).
- Screenshots saved: `docs/ux-runs/2026-09-13-1500-light-oracle/` (19 steps).

### Step-by-Step Flow & Screenshots

1. **Arrival & Examples Modal** (`01-arrival.png`):
   Arrived at `http://127.0.0.1:8090/`. The Starter Examples overlay ("Choose a starter blueprint or start blank") appeared cleanly.
2. **Blank Canvas** (`02-blank-canvas.png`):
   Dismissed modal via `Start Blank`. Canvas is empty with 0 nodes and `untitled.cf.yaml` in header.
3. **Search Kinds for "s3"** (`03-search-kinds-s3-empty.png`):
   Searching the kinds palette for "s3" yielded no results, confirming initial empty state.
4. **Search Kinds for "bucket"** (`04-search-kinds-bucket-empty.png`):
   Searching for "bucket" also yielded 0 results.
5. **Switch to SOURCES Tab** (`05-sources-tab.png`):
   Switched to `#rtabs button[data-r="src"]`. Catalogue of known providers displayed.
6. **Search Catalogue for "s3"** (`06-search-cat-s3.png`):
   Filtering the catalogue by "s3" immediately surfaced `provider-aws-s3` (`ghcr.io/crossplane-contrib/provider-aws-s3:v2.7.0`).
7. **Add Provider** (`07-provider-installing.png`):
   Clicked `+ Add` on `provider-aws-s3`. Button transitioned to loading state.
8. **Provider Installed** (`08-provider-installed.png`):
   Provider installed and schemas cached. Top notification banner appeared:
   `Installed provider-aws-s3:v2.7.0 (schemas loaded) — Open KINDS`.
9. **Switch to KINDS Tab** (`09-kinds-tab.png`):
   Switched to KINDS tab. Kinds loaded from the new provider schema.
10. **Search Kinds for "bucket"** (`10-kinds-search-bucket.png`):
    Searching for "bucket" listed all S3 bucket resources (`Bucket`, `BucketAcl`, `BucketCorsConfiguration`, etc.).
11. **Place Bucket onto Canvas** (`11-bucket-placed.png`):
    Dragged `Bucket` kind onto canvas `#cw`. Node `bucket` (`s3.aws.m.upbound.io/v1beta1`) rendered with input ports, outputs, and crossplane envelope.
12. **Select Node / Inspector Initial** (`12-inspector-initial.png`):
    Clicked node header. Inspector panel opened on the right.
13. **Inspector Essentials View** (`13-fields-view.png`):
    The Essentials view showed the essential fields for Bucket (`region`).
14. **Activate Wire Mode** (`14-wire-mode-active.png`):
    Clicked `Wire` (`data-m="w"`) on `region`. Mode switched to wire dropdown.
15. **Select `__new__` Parameter** (`15-new-param-form-expanded.png`):
    Selected `+ New parameter...` (`__new__`) from the wire dropdown. Inline parameter creation form expanded (`name`, `type`, OK, Cancel).
16. **Submit Parameter "region" & Wire** (`16-region-param-wired.png`):
    Filled name `region` and clicked OK (`button[data-npok="region"]`).
    - **Verification of CF-475**: The inline form dismissed immediately upon submit (`formDismissed=true`).
    - `.bound` chip `<- params.region` rendered with `x` (unwire) button.
    - Wire curve drawn on canvas connecting `XApp.region` to `Bucket.region`.
    - Manifest preview updated with `region: {from: params.region}`.
17. **Canvas Settled** (`17-canvas-settled.png`):
    Clicked canvas background to settle selection. Canvas layout clean and readable.
18. **Artifacts Tree** (`18-artifacts-tree.png`):
    Inspected bottom drawer: `preview · 4 files · 3 previews`.
19. **Generate Manifests** (`19-after-generate.png`):
    Clicked `Generate`. Dialog confirmation accepted.
    - Header indicator updated: `written · 4 files · 3 previews`.
    - Green banner displayed in output drawer:
      `✓ Output written to .testrun-ux/out · Apply: kubectl apply -R -f .testrun-ux/out · Package: cf package`

### Manifest Verification

All 4 files written to disk in `.testrun-ux/out`:
- `compositions/xapps.platform.example.org.yaml`:
  ```yaml
  apiVersion: apiextensions.crossplane.io/v1
  kind: Composition
  metadata:
    name: xapps.platform.example.org
  spec:
    compositeTypeRef:
      apiVersion: platform.example.org/v1alpha1
      kind: XApp
    mode: Pipeline
    pipeline:
    - step: render-resources
      functionRef:
        name: function-go-templating
      input:
        apiVersion: gotemplating.fn.crossplane.io/v1beta1
        kind: GoTemplate
        source: Inline
        options: ["missingkey=error"]
        inline:
          template: |
            {{- $spec := .observed.composite.resource.spec -}}
            ...
            apiVersion: s3.aws.m.upbound.io/v1beta1
            kind: Bucket
            metadata:
              annotations:
                {{ setResourceNameAnnotation "bucket" }}
            spec:
              forProvider:
                {{- if or (hasKey $spec "region") }}
                {{- if hasKey $spec "region" }}
                region: {{ $spec.region | quote }}
                {{- end }}
                {{- else }}
                {}
                {{- end }}
              providerConfigRef:
                kind: ClusterProviderConfig
                name: {{ $spec.providerName }}
    - step: auto-ready
      functionRef:
        name: function-auto-ready
  ```
- `xrds/xapps.platform.example.org.yaml`:
  ```yaml
  apiVersion: apiextensions.crossplane.io/v2
  kind: CompositeResourceDefinition
  metadata:
    name: xapps.platform.example.org
  spec:
    group: platform.example.org
    names:
      kind: XApp
      plural: xapps
    scope: Namespaced
    versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              properties:
                providerName:
                  type: string
                region:
                  type: string
              required: [providerName]
  ```
- `functions.yaml`: declares `function-go-templating:v0.12.0` and `function-auto-ready:v0.5.0`.
- `providerconfigs/aws.yaml`: ClusterProviderConfig scaffold for Upbound AWS v2.

Console log audit: 0 page errors, 0 uncaught exceptions.

---

## 4. Issues

No new issues filed during this run.

Existing open issues noted during review:
- **CF-479 (#388)** — *Artifacts tree labels the served blueprint from metadata.name, naming a file that is not on disk* (already filed and in-progress).
- **CF-478 (#387)** — *An annotation added in the inspector has no wire control, so a status output cannot be bound to it from the UI* (already filed and in-progress).

---

## 5. Dependabot

Audited via `gh pr list --search "author:app/dependabot"`:
**0 open pull requests.** No action needed.

---

## 6. Summary

- Preflight gates: all green (`make lint`, `make lint-strict`, `go test -short`).
- CI status: `main` CI green.
- Recent closures: CF-477 and CF-476 re-verified against unit and acceptance tests; both verified fixed.
- Canvas mission: Mission M1 executed end-to-end to Generate with all 19 screenshots recorded.
- Generated output: byte-deterministic, valid Crossplane v1/v2 manifests matching XRD parameter wiring contract.
