# Deep oracle run — 2026-09-13 06:12 UTC

Slot `(13*4 + 6/7) % 3 = 1` → missions **M2 (Status Wire / IRSA) + M3 (Connection Secret / RDS PostgreSQL)**, plus evaluation of **Essentials Form & Manifest Editor** (CF-470, CF-471, CF-473). Engine on port **8090**, seeded schema cache in `~/Library/Caches/compositionfactory`. Blank start; no report file existed beforehand.

Driver: scripted Playwright running against Google Chrome for Testing (Chromium 1243, arm64). Blank starter modal suppressed via `localStorage.setItem('cf:empty-start-offered', '1')` and explicit `#examplesOverlay` dismissal. Console errors, page errors, dialogs, and HTTP responses logged in `events.log`; 33 screenshots captured in `docs/ux-runs/2026-09-13-0912-deep-oracle/`.

Oracle only: no code fixed, no issue closed, no `verified` or `in-progress` label added, nothing pushed to `main`.

---

## 1. Gates

All five mandatory gates were executed locally and passed with zero errors or warnings.

| Gate | Command | Result | Verdict |
|---|---|---|---|
| `make lint` | `gofmt` & `go vet ./...` | Clean formatting, 0 vet errors | **PASS** |
| `make lint-strict` | `staticcheck@v0.8.1 ./...` | 0 issues across all packages | **PASS** |
| `test-short` | `go test ./... -short -count=1` | All packages `ok`, 0 failures | **PASS** |
| `make test-race` | `go test ./... -short -race -count=1` | All packages `ok`, 0 race conditions | **PASS** |
| `govulncheck` | `govulncheck ./...` | No vulnerabilities affecting code | **PASS** |

---

## 2. Dependabot PR Audit

Executed audit query via `gh pr list --search "author:app/dependabot"`:

- **Open Dependabot PRs**: **0**
- No pending dependency bumps, merge conflicts, or CI failures.
- Correlated with `govulncheck ./...`: zero active vulnerabilities in module dependencies.

---

## 3. Verification of Recent Closes

Window: issues closed in the previous 8 hours (between 2026-09-12T22:12Z and 2026-09-13T06:12Z). All 12 recent closures were re-verified against their guarding unit, acceptance, or E2E tests:

| Issue | Task ID | Description | Guarding Test | Result |
|---|---|---|---|---|
| [#375](https://github.com/koorikla/composition-factory/issues/375) | CF-473 | Manifest editor error line select, snippet insertion & no-op apply | `tests/cf471-manifest-editor.spec.js` | **PASS** |
| [#370](https://github.com/koorikla/composition-factory/issues/370) | CF-472 | Defer undeclared cached providers when sources declared | `internal/api/providers_test.go` (`TestCF472*`) | **PASS** |
| [#367](https://github.com/koorikla/composition-factory/issues/367) | CF-470 | Essentials form per kind with expose-as-parameter | `tests/cf470-essentials-form.spec.js` | **PASS** |
| [#366](https://github.com/koorikla/composition-factory/issues/366) | CF-469 | Field coverage helpers modularization | `tests/cf141-rds-postgres.spec.js` | **PASS** |
| [#365](https://github.com/koorikla/composition-factory/issues/365) | CF-467 | Center initial view on blank blueprints | `tests/cf467-canvas-centering.spec.js` | **PASS** |
| [#364](https://github.com/koorikla/composition-factory/issues/364) | CF-467 | Show only declared sources as installed in catalogue | `tests/cf467-sources-installed-providers.spec.js` | **PASS** |
| [#363](https://github.com/koorikla/composition-factory/issues/363) | CF-466 | Preserve declared environment key types on adopt | `internal/adopt/adopt_test.go` (`TestCF466*`) | **PASS** |
| [#361](https://github.com/koorikla/composition-factory/issues/361) | CF-465 | Visually reflect background provider downloads | `tests/cf465-provider-background-fetch.spec.js` | **PASS** |
| [#360](https://github.com/koorikla/composition-factory/issues/360) | CF-464 | Decouple synchronous source fetches & timeout | `internal/api/source_fetch_test.go` (`TestCF464*`) | **PASS** |
| [#358](https://github.com/koorikla/composition-factory/issues/358) | CF-462 | Reconcile missing schema kinds | `internal/api/kinds_test.go` (`TestCF462*`) | **PASS** |
| [#357](https://github.com/koorikla/composition-factory/issues/357) | CF-461 | Provider catalogue search and filtering | `tests/cf461-catalogue-search.spec.js` | **PASS** |
| [#353](https://github.com/koorikla/composition-factory/issues/353) | CF-459 | Deduplicate kind definitions across versions | `internal/api/catalogue_test.go` (`TestCF459*`) | **PASS** |

Zero regressions found. All 12 issues remain cleanly fixed and guarded.

---

## 4. Drift Spot-Check

Tested `cf gen` with the `--check` flag on sample blueprints to verify determinism and ensure no drift between the CLI generator and intermediate representations:

1. **IRSA Blueprint (`internal/examples/irsa.cf.yaml`)**:
   - Manifests generated: `compositions/xirsas.platform.sparky.ee.yaml`, `functions.yaml`, `providerconfigs/aws.yaml`, `xrds/xirsas.platform.sparky.ee.yaml`.
   - `cf gen internal/examples/irsa.cf.yaml --check -o /tmp/cf-gen-1` exited `0` (`in sync`).
2. **RDS Blueprint (`internal/examples/rds.cf.yaml`)**:
   - Manifests generated: `compositions/xpostgresinstances.database.sparky.ee.yaml`, `functions.yaml`, `providerconfigs/aws.yaml`, `xrds/xpostgresinstances.database.sparky.ee.yaml`.
   - `cf gen internal/examples/rds.cf.yaml --check -o /tmp/cf-gen-rds` exited `0` (`in sync`).

**Verdict**: 100% deterministic byte-identical generation; zero drift.

---

## 5. Canvas Missions

### Mission M2: The Status Wire (IRSA) — **COMPLETED**

- **Flow**:
  1. Started from blank canvas (`01-m2-blank-arrival.png`).
  2. Switched to SOURCES tab (`02-m2-sources-tab.png`), searched catalogue for `iam` (`03-m2-sources-search-iam.png`), clicked **Add** on `provider-aws-iam` (`04-m2-sources-iam-added.png`).
  3. Switched to KINDS tab, filtered by `Role` (`05-m2-kinds-filter-role.png`), dropped `Role` onto canvas (`06-m2-role-dropped.png`).
  4. Filtered kinds by `ServiceAccount` (`07-m2-kinds-filter-sa.png`), dropped `ServiceAccount` onto canvas (`08-m2-sa-dropped.png`).
  5. Selected `ServiceAccount` card to open Inspector (`09-m2-sa-inspector.png`).
  6. Added annotation `eks.amazonaws.com/role-arn` (`10-m2-sa-annotation-added.png`).
  7. Connected status wire from `Role` output `status.atProvider.arn` to `ServiceAccount` annotation `eks.amazonaws.com/role-arn` (`11-m2-wire-connected.png`).
  8. Clicked Tidy Layout button (`#layout-btn`) to arrange graph left-to-right (`12-m2-after-layout.png`).
  9. Clicked **Validate**; canvas reported green validation status (`13-m2-validate-status.png`).
  10. Clicked **Generate**; confirmed output directory overwrite dialog; written 4 manifest artifacts: `compositions`, `functions.yaml`, `providerconfigs`, `xrds` (`14-m2-generate-status.png`).

### Mission M3: Connection Secret (RDS PostgreSQL) — **COMPLETED**

- **Flow**:
  1. Reset to blank canvas (`15-m3-blank-arrival.png`).
  2. Switched to SOURCES, searched `rds` (`16-m3-sources-search-rds.png`), clicked **Add** on `provider-aws-rds` (`17-m3-sources-rds-added.png`).
  3. Switched to KINDS, filtered `Instance` (`18-m3-kinds-filter-instance.png`), dropped `Instance` onto canvas (`19-m3-instance-dropped.png`).
  4. Opened Inspector on `Instance` (`20-m3-instance-inspector.png`).
  5. Configured forProvider fields:
     - `region: "us-east-1"`
     - `engine: "postgres"`
     - `engineVersion: "15.4"`
     - `instanceClass: "db.t4g.micro"`
     - `allocatedStorage: "20"`
     - `username: "master"`
     - `autoGeneratePassword: "true"`
     - `skipFinalSnapshot: "true"`
     - `passwordSecretRef.name: "db-master-password"`
     - `passwordSecretRef.key: "password"`
  6. Configured Crossplane Envelope:
     - `writeConnectionSecretToRef.name` wired from `params.connectionSecretName` (`21-m3-instance-configured.png`).
  7. Added composite parameter `connectionSecretName` with default `"rds-conn-secret"`.
  8. Clicked **Validate**; verified valid status with no schema errors (`22-m3-validate-status.png`).
  9. Clicked **Generate**; written full suite of Crossplane manifests (`23-m3-generate-status.png`).

### Extra Area: Essentials Form & Manifest Editor (CF-470, CF-471, CF-473) — **COMPLETED**

- **Flow**:
  1. Reset to blank canvas (`24-extra-blank-arrival.png`).
  2. Searched and dropped native Kubernetes `Deployment` (`25-extra-deployment-dropped.png`).
  3. Clicked card header to inspect (`26-extra-deployment-inspector-default.png`).
  4. Confirmed Manifest view is the active default view per CF-471.
  5. Toggled to Fields view (`#vseg button[data-view="fields"]`) (`27-extra-fields-view.png`).
  6. Evaluated Essentials form section per CF-470 (`28-extra-essentials-form.png`), showing key fields (`replicas`, container specs).
  7. Tested **Expose as Parameter** on `spec.replicas` (`29-extra-replicas-exposed.png`).
  8. Switched back to Manifest view (`30-extra-manifest-view.png`).
  9. Opened Manifest Editor via `data-manifest-edit` button.
  10. Tested Snippet dropdown (`select[data-manifest-snippet]`) inserting starter container snippet into YAML (`31-extra-snippet-selected.png`).
  11. Tested Apply button: verified server CRD schema validation rejects malformed/incomplete snippet edits with clean HTTP 400 (`did not find expected key`) and keeps the editor open while highlighting the erroneous line (`33-extra-after-apply.png`), guarding the document from corruption.
  12. Tested Inspector Search (`#insp-search`) querying `containerPort` (`32-extra-manifest-search.png`), highlighting matching fields across the resource hierarchy.

---

## 6. Findings

**0 new issues filed.**
All tested workflows, gates, and recent fixes executed cleanly without regressions or uncaught exceptions.

---

## 7. What Worked Well

- **Schema Gate Defense**: In the Manifest Editor, server-side CRD and YAML schema validation prevents invalid syntax or unknown keys from corrupting the blueprint IR, highlighting the error line directly in the editor drawer without discarding user changes.
- **Envelope Wiring Ergonomics**: Wiring `writeConnectionSecretToRef.name` to a parameter is directly supported in the resource envelope, allowing seamless connection secret credential exports in RDS compositions.
- **Status Wire Automatic Guards**: Emitted Go-template compositions automatically generate `dig` and `hasKey` conditionals for status-wired fields, preventing rendering panics before the provider populates resource status.
- **Provider Catalogue Add**: Installing providers from the local cache is instantaneous, immediately populating the Kinds palette with grouped CRD resources and badge counts.
