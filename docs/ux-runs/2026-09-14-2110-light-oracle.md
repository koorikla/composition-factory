# Light oracle run — 2026-09-14 21:10 UTC

Slot: `UTC_hour/7` = `20/7` = 2 → **M3 (Connection secret: RDS PostgreSQL)**. Started 20:55 UTC, budget 75 min.
Checkout: `5f37319` (origin/main). Engine port **8090** (isolated per `AGENTS.md` §2). Mission carried to Generate.

Oracle only: no code fixed, no issue closed, no `verified`/`in-progress` label added, nothing pushed directly to `main`.

---

## 1. Gates

| gate | here | main CI (run 34895875255 on `fb11f14` / 34897070001 on `5f37319`) | verdict |
|---|---|---|---|
| `make lint` | **pass** (`gofmt`, `go vet`, `eslint`, `tsc` clean) | success | green |
| `make lint-strict` | **pass** (staticcheck v0.8.1 clean) | success | green |
| `make test` (`go test ... -short -count=1`) | **pass** (all packages ok) | success | green |

All requested gates (`lint`, `lint-strict`, `test-short`) passed with 0 errors locally.
Main CI on commit `fb11f14` (`fix(web): search inspector envelope rows and prevent false no-match (CF-484)`) completed all 5 jobs clean (`acceptance`, `test`, `cluster`, `docker-build`, `e2e`).
Main CI run 34897070001 on commit `5f37319` (`fix(emit): preserve operator-supplied environment values across regeneration (CF-487)`) is in progress.

---

## 2. Verification of recent closes

Window: 4 hours (since 16:55 UTC).
We audited the 5 most recent closed issues / landed PRs, including all issues merged in this cycle:

| issue | id | how verified | verdict |
|---|---|---|---|
| #400 | CF-487 | `go test ./cmd/cf -run TestCF487 -v` (3/3 tests pass) | verified fixed |
| #396 | CF-484 | Live canvas Inspector search on port 8090: searching `"secret"` matches `writeConnectionSecretToRef.name` and envelope rows without false no-match | verified fixed |
| #395 | CF-483 | `npx playwright test tests/cf483-blueprint-tab-label.spec.js` | verified fixed |
| #394 | CF-482 | `go test ./internal/emit -run StatusWire -v` | verified fixed |
| #391 | CF-480 | `go test ./internal/examples -v` | verified fixed |

### Details

- **CF-487 (#400)**: *cf gen blanks an operator-supplied environment value in environmentconfigs/default.yaml at exit 0*.
  Landed in commit `5f37319`. Verified via `go test ./cmd/cf -run TestCF487 -v`:
  - `TestCF487GenDoesNotSilentlyBlankOperatorEnvironmentValue`: PASS (0.02s)
  - `TestCF487BlueprintDefaultWinsOverExistingValue`: PASS (0.00s)
  - `TestCF487BlueprintDataWinsOverExistingValue`: PASS (0.00s)
- **CF-484 (#396)**: *Search inspector envelope rows and prevent false no-match*.
  Landed in commit `fb11f14`. Exercised live during the M3 canvas mission: searching `"secret"` in `#insp-search` returns 20 schema hits including `writeConnectionSecretToRef.name` and `passwordSecretRef.{key,name}`, without incorrectly showing `"No schema field matches"`.
- **CF-483 (#395)**: *The output region's blueprint tab still names the file from metadata.name after CF-479 fixed the tree row*.
  Landed in commit `26b38c2`. Guarding test `tests/cf483-blueprint-tab-label.spec.js` passed clean.
- **CF-482 (#394)**: *A status wire in an annotation does not gate its resource*.
  Landed in commit `cf876cb`. Guarding test in `internal/emit/render_test.go` passed clean.
- **CF-480 (#391)**: *RDS starter omits username and passwordSecretRef*.
  Landed in commit `cece0b4`. Guarding test in `internal/examples/examples_test.go` passed clean.

---

## 3. Dependabot PR audit

Queried open Dependabot PRs via `gh pr list --search "author:app/dependabot"`:
**0 open PRs**. All dependencies are up to date.

---

## 4. Canvas UX Mission: M3 (Connection secret: RDS PostgreSQL)

### Setup and Environment
- Port: `8090` (UX tester port).
- Cache directory seeded from fixtures for AWS S3 and SQS; `provider-aws-rds:v2.7.0` fetched and cached dynamically into `.testrun-ux/cache/provider-aws-rds-8ea8ed0acc5c`.
- Output directory: `.testrun-ux/out`.

### Narrative & Observations (Naive-Eye Review)

1. **Blank Arrival & Starter Discovery**:
   - On launching the visual canvas with an empty blueprint (`metadata: { name: "blank-db" }`), the canvas immediately and automatically opens `#examplesOverlay` (`Starter Blueprints: Choose an Example Blueprint`).
   - Cards display clear visual status badges:
     - Cloud-agnostic and zero-provider starters (`k8s-workload`, `cloud-database`, etc.) display `● ready`.
     - Starters needing downloads display a badge showing the exact missing count (e.g. `↓ 1 provider`).
   - The card for `AWS RDS PostgreSQL Database` summarizes: *"Production-grade RDS PostgreSQL instance with storage autoscaling, backup retention, engine versioning, deletion protection, and credentials connection secret envelope."*

2. **Loading the RDS PostgreSQL Starter**:
   - Clicking `Load Blueprint` on the RDS card triggers schema retrieval. The button transitions to `Loading…` state while fetching the provider schema.
   - Once retrieved, the modal dismisses automatically and the canvas populates with two cards:
     - `XPostgresInstance` (XRD composite definition node with 12 parameters: `allocatedStorage`, `backupRetentionPeriod`, `dbName *`, `deletionProtection`, `engineVersion`, `instanceClass`, `maxAllocatedStorage`, `multiAz`, `providerName *`, `publiclyAccessible`, `region`, `username`).
     - `Instance` (`db-instance`, `rds.aws.m.upbound.io/v1beta1`) with 155 leaf schema fields and Crossplane envelope wiring.
   - 9 wires connect parameters directly to the managed resource's `spec.forProvider` fields.

3. **Inspector & Secret Discoverability (CF-484 verification)**:
   - Selecting `db-instance` displays the Inspector panel in the right sidebar with view selector `[Manifest] [Fields]` and search box `#insp-search`.
   - **Searching `"secret"`**:
     - Manifest view lists 20 search hits, including:
       - `writeConnectionSecretToRef.name` (type `string`)
       - `passwordSecretRef.key` (type `string`)
       - `passwordSecretRef.name` (type `string`)
       - `passwordWoSecretRef.name` (type `string`)
       - `masterUserSecretKmsKeyIdRef.name` (type `string`)
     - CF-484 is verified live: the envelope property `writeConnectionSecretToRef.name` is surfaced directly in search hits even though it resides outside `spec.forProvider`. A developer searching for "secret" discovers how connection secrets work without needing prior knowledge of Crossplane's specific "envelope" vocabulary.
   - **Crossplane Envelope Section**:
     - Scrolling down the inspector reveals the dedicated `Crossplane Envelope` section (`Secrets, policies and metadata outside forProvider:`).
     - `writeConnectionSecretToRef.name` is configured with wire binding `← params.dbName`.
     - `passwordSecretRef` is configured via `passwordSecretRef.name: "rds-master-pass"` and `passwordSecretRef.key: "password"`. At no point is a plaintext password entered or exposed in plain text.

4. **Generation & Manifest Verification**:
   - Clicking `Generate` displays the confirmation dialog: *"Generate will write manifests to disk in '.testrun-ux/out'. Proceed?"*.
   - Output files written to `.testrun-ux/out`:
     - `compositions/xpostgresinstances.database.sparky.ee.yaml`
     - `xrds/xpostgresinstances.database.sparky.ee.yaml`
     - `functions.yaml`
     - `providerconfigs/test.yaml`
   - Generated Composition verification:
     - Carries the connection secret envelope:
       ```yaml
       writeConnectionSecretToRef:
         name: {{ $spec.dbName | quote }}
       ```
     - Password references the Kubernetes secret reference safely:
       ```yaml
       passwordSecretRef:
         key: 'password'
         name: 'rds-master-pass'
       ```
     - **Verification**: The password never appears as a literal string in the composition output.
   - Canvas top status bar updates to: `● written · 4 files · 3 previews` with confirmation toast: `✓ Output written to .testrun-ux/out · Apply: kubectl apply -R -f .testrun-ux/out · Package: cf package`.
   - Zero console errors and zero page errors observed throughout the entire session.

---

## 5. Issues filed

**0 issues filed**. All features behaved according to specifications, and all tested recent defect fixes remain intact.
