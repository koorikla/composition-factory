# Architectural Addendum — 2026-09-02

## 1. System Overview & Technology Stack

- **Engine Core (`internal/emit`)**: Pure Go compiler emitting byte-identical, deterministic Crossplane Composition, XRD, functions.yaml, and ProviderConfig artifacts across CLI (`cf gen`), HTTP API (`cf serve`), and stdio MCP (`cf mcp`).
- **Intermediate Representation (`factory.crossplane.io/v1alpha1 Blueprint`)**: Single source of truth.
- **Frontend (`web-proto`)**: Native ES module canvas architecture with pure DOM/SVG rendering, zero build-step requirement, and sub-millisecond local interaction cycles.
- **Multi-Engine Emission**:
  - `go-templating`: Go-templating functions (`function-go-templating`) with support for inline and FileSystem folder export (`spec.emit.templateSource: FileSystem`).
  - `kcl`: KCL functions (`function-kcl` with typed `KCLInput`).
  - `python`: Python functions (`function-python` with typed `fnv1` RunFunctionRequest/Response).
- **Packaging & Adoption**:
  - `cf package` & `cf push`: OCI Crossplane Configuration (`.xpkg`) packaging and distribution.
  - `cf adopt`: Ingestion and reverse-compilation of existing Crossplane compositions into canonical blueprints.

## 2. Milestone Status

- **M1 (Engine Core & Schema Validation)**: Complete. Strict OpenAPI schema validation, deterministic YAML emission, and parameter/wire compilation.
- **M2 (CLI & Provider Cache)**: Complete. `cf gen`, `cf provider add`, `.cf.lock`, and OCI layer fetching.
- **M3 (Visual Canvas & Discovery)**: Complete. Interactive canvas, drag-to-wire, provider catalogue (476 packages), and live cluster discovery.
- **M4 (Adoption & Extensions)**: Complete. `cf adopt` for patch-and-transform / go-templating, `crds:` manifest sources, and starter blueprints.
- **M5 (Multi-Engine & Production Readiness)**: Core deliverables complete (`function-kcl`, `function-python`, `templateSource: FileSystem`, `cf package` / `cf push`). (Note: Standalone `cf index export` and Cosign signature verification remain deferred roadmap extensions).
