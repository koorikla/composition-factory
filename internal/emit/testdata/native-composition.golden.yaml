# Generated by compositionfactory. Do not edit.
# Source: blueprints/xwebapp.cf.yaml
# Regenerate with: cf gen
apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: xwebapps.platform.sparky.ee
spec:
  compositeTypeRef:
    apiVersion: platform.sparky.ee/v1alpha1
    kind: XWebApp
  mode: Pipeline
  pipeline:
  - step: render-resources
    functionRef:
      name: function-go-templating
    input:
      apiVersion: gotemplating.fn.crossplane.io/v1beta1
      kind: GoTemplate
      source: Inline
      options: ["missingkey=error"]
      inline:
        template: |
          {{- $spec := .observed.composite.resource.spec -}}
          {{- $xr := .observed.composite.resource.metadata.name -}}
          {{- $xrMeta := .observed.composite.resource.metadata -}}
          ---
          apiVersion: sqs.aws.m.upbound.io/v1beta1
          kind: Queue
          metadata:
            annotations:
              {{ setResourceNameAnnotation "main-queue" }}
          spec:
            forProvider:
              region: 'eu-north-1'
            providerConfigRef:
              kind: ClusterProviderConfig
              name: {{ $spec.providerName }}
          ---
          apiVersion: apps/v1
          kind: Deployment
          metadata:
            annotations:
              {{ setResourceNameAnnotation "web" }}
          spec:
            {{- if hasKey $spec "replicas" }}
            replicas: {{ $spec.replicas }}
            {{- end }}
            selector:
              matchLabels: {app: web}
            template:
              metadata:
                labels: {app: web}
              spec:
                containers:
                  - image: {{ $spec.image }}
                    name: 'web'
          ---
          apiVersion: v1
          kind: Service
          metadata:
            annotations:
              {{ setResourceNameAnnotation "web-svc" }}
          spec:
            ports:
              - port: 80
            selector: {app: web}
            type: 'ClusterIP'
  - step: auto-ready
    functionRef:
      name: function-auto-ready
