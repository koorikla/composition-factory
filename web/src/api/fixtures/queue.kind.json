{
  "kind": {
    "kind": "Queue",
    "group": "sqs.aws.m.upbound.io",
    "version": "v1beta1",
    "apiVersion": "sqs.aws.m.upbound.io/v1beta1",
    "plural": "queues",
    "scope": "Namespaced",
    "provider": "xpkg.upbound.io/upbound/provider-aws-sqs:v2.7.0",
    "namespaced": true,
    "required": 1,
    "fields": 18
  },
  "envelope": [
    {
      "path": "deletionPolicy",
      "type": "string",
      "description": "DeletionPolicy specifies what will happen to the underlying external when this managed resource is deleted - either \"Delete\" or \"Orphan\" the external resource.",
      "required": false,
      "depth": 0
    },
    {
      "path": "managementPolicies",
      "type": "array",
      "description": "ManagementPolicies specify the array of actions Crossplane is allowed to take on the managed and external resources. This field is planned to replace the deletionPolicy field.",
      "required": false,
      "depth": 0
    },
    {
      "path": "providerConfigRef.name",
      "type": "string",
      "description": "Name of a provider config, referenced by the resource being provisioned.",
      "required": true,
      "depth": 1
    },
    {
      "path": "publishConnectionDetailsTo.name",
      "type": "string",
      "description": "Name is the name of the connection secret.",
      "required": true,
      "depth": 1
    },
    {
      "path": "writeConnectionSecretToRef.name",
      "type": "string",
      "description": "Name of the secret.",
      "required": true,
      "depth": 1
    },
    {
      "path": "writeConnectionSecretToRef.namespace",
      "type": "string",
      "description": "Namespace of the secret.",
      "required": true,
      "depth": 1
    }
  ]
}
